apiVersion: v2
name: urbalytix-chart
description: A Helm chart to deploy Starwit Urbalytix to Kubernetes

type: application

version: ${project.version}

appVersion: "${project.version}"

dependencies:
  - name: postgresql
    version: "16.7.21"
    repository: "oci://registry-1.docker.io/bitnamicharts"
